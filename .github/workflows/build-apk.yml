name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📂 Checkout code
      uses: actions/checkout@v4

    - name: 🟢 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: ☕ Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: 📦 Install Project Dependencies
      run: npm install --legacy-peer-deps

    - name: 🛠️ Patch Expo Plugins
      # تم حل مشكلة الصيغة التي ظهرت في الصورة 1000066332
      run: npx expo install expo-build-properties -- --non-interactive

    - name: ⚙️ Expo Prebuild
      run: npx expo prebuild --platform android --clean

    - name: 🏗️ Fix Android Repositories (The Final Fix)
      # هذا الأمر يحل مشكلة الصورة 1000066341 بإضافة JitPack يدوياً لكل ملفات Gradle
      run: |
        find android -name "build.gradle" -exec sed -i "s/google()/google()\n        mavenCentral()\n        maven { url 'https:\/\/jitpack.io' }/g" {} +

    - name: 🔐 Set Execute Permission
      run: chmod +x android/gradlew

    - name: 🏗️ Build Android APK (Release)
      run: |
        cd android
        ./gradlew assembleRelease --no-daemon \
          -Pandroid.mergeResources.failOnErrors=false \
          -Dorg.gradle.warning.mode=none
        cd ..

    - name: 📤 Upload L'WESMOU OS APK
      uses: actions/upload-artifact@v4
      with:
        name: LWESMOU-OS-Build
        path: android/app/build/outputs/apk/release/*.apk
        retention-days: 7
        
